# ──── Server ────
server.port=8080

# ──── MySQL ────
spring.datasource.url=jdbc:mysql://localhost:3306/wellfitness_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=159Atg45@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ──── JPA / Hibernate ────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=CHAR

# ──── JWT ────
app.jwt.secret=Wellfitness_Super_Secret_Key_2025_Change_In_Production_MinLength64Chars!!
app.jwt.expiration-ms=2592000000

# ──── File Upload ────
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ──── Google OAuth ────
app.google.client-id=473083426303-lo1u95qd7ptter1pshqe3eaqqutbqgkl.apps.googleusercontent.com

# ──── OTP ────
app.otp.expiry-minutes=5

# ──── Gmail SMTP (for email verification) ────
# ⚠️ Replace with your Gmail + App Password
# To create App Password: Google Account → Security → 2-Step Verification → App Passwords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=YOUR_GMAIL@gmail.com
spring.mail.password=YOUR_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
