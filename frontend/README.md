# Frontend (Vite + React)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

Notes:

- Components were reorganized under `src/components/{layout,ui,workout}`.
- Services live in `src/services`.
